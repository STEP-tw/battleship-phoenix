const Player = require('./../../src/models/player.js');
const assert = require('chai').assert;

// describe('Player', () => {
//   describe('getPlayerId', () => {
//     it('should return player id that player has', () => {
//       let player = new Player(1);
//       assert.equal(player.getPlayerId(), 1);
//     });
//   });
// });
